# 光影实验室 · Light Studio

基于 **Three.js**（v0.160，本地依赖，无需联网）的 3D 光影演示与调节工具。可自由选择展示
立方体、球体、圆柱、圆锥、四面体、八面体、二十面体、十二面体、圆环结等标准 3D 模型，
并实时调节光照环境。

## ✨ 功能特性

### 模型（左侧面板）
- 9 种标准模型：立方体 / 球体 / 圆柱 / 圆锥 / 四面体 / 八面体 / 二十面体 / 十二面体 / 圆环结
- **自由选择展示哪些模型、展示几个**：点亮按钮即可增减，多模型按占地宽度自动排开居中；
  点击 3D 场景中的模型选中编辑（地面金色圆环指示，不随模型旋转、不闪烁）
- **朝向调整**：绕 X / Y / Z 轴任意旋转（0–360°），一键重置朝向（多面体默认一个平面贴地，
  旋转后模型自动贴地）
- **「吸附面朝地」**：开启后点击模型表面，该面将转至水平并**朝向地面**（模型骑坐在该面上）
- **「锉边宽度」**：把模型上的硬棱（相邻面夹角 ≥ 25° 的凸棱）磨成一条很窄的斜面，
  就像用锉刀锉过一样 —— 棱上会出现一块独立朝向的窄面，光照下的高光与明暗过渡因此清晰可见；
  0 为关闭（默认 0.03）。平滑面（球面、圆柱侧面等夹角小的面）不受影响，模型仍是圆滑的
- **所有参数均可直接输入数值**（滑杆与数字输入框双向同步）；可一键「重置模型参数」（保留朝向）
- 其他可调参数：颜色、金属度、粗糙度、X/Y/Z 三轴缩放比例、球面/径向/管状细分段数、
  圆柱顶面半径比（0=圆锥，1=正圆柱）、多面体细节层级、**边线显示**（在实体表面叠加边缘线，
  颜色随模型颜色自动加深，方便观察边的光影）、线框显示、自转速度（默认不旋转）

### 光照（右侧面板）
- **光源数量**：1–6 个可独立配置的光源
- 每个光源支持：
  - 类型切换：平行光 / 点光源 / 聚光灯
    （点光源 = 四周发散的圆形光斑；聚光灯 = 定向锥形光束，**锥角/羽化**控制光束集中度与边缘软硬，
    地面受光后两者的光斑形状差异一目了然）
  - **色温**（1000K–12000K，开尔文 → RGB 实时换算）或自定义颜色
  - **强度**、**方位角**、**仰角**、**距离**，
    所有参数滑杆与数值输入框双向同步，可直接输入精确值
  - 启用开关、投射阴影开关
- 全局：环境光强度/色温、半球光、ACES 色调映射曝光度、**环境反射强度**、4 种背景
  （地面随背景自动配色并**接收光照**，点/聚光的光斑、阴影都直接可见）
- **实时环境反射（IBL）**：用 `CubeCamera` 把当前场景**实时**捕捉成立方体贴图
  （含光源标记、地面、参考网格、天地渐变），经 `PMREMGenerator` 预滤波后作为环境贴图。
  因此：
  - 金属表面**真实反射光源** —— 光源挪到哪儿，镜面里的光点就跟到哪儿；
  - **粗糙度**越低反射越清晰锐利、越高越模糊（光点被糊开、地平线变柔），差别一眼可见；
  - 地面、网格、相邻模型都会被照进金属里
  「环境反射」滑杆整体调节这层反射的强弱（0 即关闭），6 套预设各带合适强度
- 场景中的光源标记（光球、方向箭头、锥形光束线）实时随参数移动，可一键隐藏
- 6 套灯光预设：影棚标准 / 正午日光 / 日出黄昏 / 清冷月光 / 舞台霓虹 / 影棚柔光
  （每套预设自带合适的环境反射强度）

### 其他
- 轨道相机：拖拽旋转、滚轮缩放、右键平移
- **视角控制坞（画面中间底部，三栏并排）**：左「预设视角」6 个默认视角一键切换；
  中「视角参数」方位角/仰角/距离滑杆与数值输入双向同步、鼠标拖动实时跟随；
  右「自定义视角」保存/应用/内联重命名/删除，持久保存
- **保存截图带参数**：截图左上角叠加视角/光照/模型/背景等参数信息，
  文件名同时携带这些参数（如 `light-studio_AZ45_EL30_D12.0_L2_暗色影棚_时间.png`）
- **「保存为默认配置」**：一键把当前全部设置存为默认配置，下次启动自动加载；
  「重置全部设置」恢复为已保存的默认（未保存过则恢复出厂预设）
- 自动旋转、参考网格、全屏、FPS 显示
- 设置自动保存到浏览器 localStorage（刷新/重启浏览器不丢失）

## 🚀 运行方式

> 页面使用 ES Module，直接双击 `index.html`（file:// 协议）无法运行，需通过本地服务器访问。

**方式一（推荐）**：双击 `光影工作室.bat` —— 自动挑选空闲端口，
**先立即用系统默认浏览器打开页面（`start` 命令），随后再启动服务器**
（浏览器启动耗时正好覆盖服务器启动，页面加载时服务已就绪）。
（服务器依次尝试 Node.js → Python → npx）

**方式二**：手动启动，任选其一：

```bash
node serve.js          # 自动选择空闲端口，监听成功后自动打开浏览器（多重策略兜底）
# 或
python -m http.server 8321 --bind 127.0.0.1
# 或
npx http-server -p 8321
```

然后访问控制台打印的地址（默认 **http://127.0.0.1:8321/** ）。
> 若不希望自动打开浏览器（Node 方式），设置环境变量 `OPEN_BROWSER=0` 即可。

## 📁 目录结构

```
light-studio/
├── index.html          页面结构
├── styles.css          界面样式
├── app.js              3D 场景与全部交互逻辑
├── bevel.js            锉边（把硬棱磨成很窄的斜面，几何算法）
├── serve.js            零依赖静态服务器（Node）
├── package.json
├── 光影工作室.bat       一键启动脚本
├── tools/              开发用自检脚本（不参与页面运行）
│   ├── bevel-test.mjs      锉边几何自测：水密性 / 法线 / 体积 / 耗时
│   ├── render-check.mjs    渲染数值自检：无头 Chrome 截图 + 像素差分断言
│   ├── png-stats.mjs       PNG 解码与画面统计（纯 Node，无依赖）
│   └── seed.html           自检用：把配置写进 localStorage 再跳转主页面
└── vendor/
    ├── three.module.js      Three.js r160（本地离线依赖）
    └── OrbitControls.js     轨道相机控制器
```

### 自检脚本（开发用）

```bash
node tools/bevel-test.mjs     # 锉边几何：水密性/法线/体积/耗时，22 个用例

# 渲染自检需要静态服务器在 8399 端口
PORT=8399 OPEN_BROWSER=0 node serve.js &
node tools/render-check.mjs   # 无头 Chrome 截图 → 像素差分断言参数确实影响画面
```

> 渲染自检必须走 GPU（不要加 `--disable-gpu`），软件渲染每张图要 1–2 分钟。

## 🛠 技术说明

- 渲染：WebGL + Three.js r160，ACES Filmic 色调映射，PCF 软阴影
- 光照模型：经典强度标定（0–10 直观量级），点/聚光灯带 25m 截止距离与平方衰减，
  三种光源类型亮度表现一致、兼容性最佳
- 色温换算：Tanner Helland 近似算法（1000K 烛光 → 12000K 蓝天）
- 环境反射：`CubeCamera`（256²，半浮点）实时捕捉场景 → `PMREMGenerator.fromCubemap`
  预滤波 → `scene.environment`；捕捉时隐藏模型自身避免自反射反馈，光源标记强制参与
  并临时提亮，形成清晰的镜面光点；场景变化时才重算（限频 5 次/秒，实测满帧无压力）
- 锉边（`bevel.js`）：顶点焊接 → 共面三角形合并成多边形面 → 判定凸硬棱 →
  每个面按「边内缩」求角点（未倒角的棱两侧共用同一角点，避免发丝裂缝）→
  输出「内缩面片 + 斜面 + 顶点帽」，天然水密；平滑面之间按 35° 夹角焊接法线，
  因此圆柱侧面依旧圆滑、只有棱上的窄斜面是硬边
- 无构建步骤、无外部 CDN，开箱即用

## 🗂 版本管理

- 本目录为 Git 仓库（默认分支 `main`），vendor/ 离线依赖一并提交，克隆后开箱即用
- 常用命令：`git status` / `git add -A` / `git commit -m "说明"` / `git log --oneline`
